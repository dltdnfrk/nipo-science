{
  "schema_version": 1,
  "classification_levels": ["public", "internal", "confidential", "restricted", "secret"],
  "fields": [
    {"id": "provider_api_key", "category": "provider_credentials", "sensitive": true, "classification": "secret", "retention": "until the researcher removes the provider; stored only as AES-256-GCM ciphertext bound to its provider", "egress": "one authorization header of one request to the explicitly selected provider endpoint", "redaction": "never in argv, logs, reprs, tracebacks, or durable records; ciphertext and provider id only"},
    {"id": "local_session_token", "category": "local_interface", "sensitive": true, "classification": "secret", "retention": "regenerated per server start; owner-only token file inside the data root", "egress": "loopback-only requests between the local front end and the loopback API", "redaction": "redacted in reprs; never logged or exported"},
    {"id": "download_signing_key", "category": "local_interface", "sensitive": true, "classification": "secret", "retention": "owner-only key file inside the data root; disclosed divergence until sealed like provider keys", "egress": "never leaves the machine; signs local download URLs only", "redaction": "value never logged; key path and rotation events only"},
    {"id": "scientific_inputs", "category": "research", "sensitive": true, "classification": "confidential", "retention": "kept in the researcher-owned data root until the researcher deletes or archives the project", "egress": "only the model turn the researcher explicitly initiated toward the selected provider", "redaction": "never in operational logs; digests and identifiers only"},
    {"id": "artifact_outputs", "category": "research", "sensitive": true, "classification": "confidential", "retention": "immutable digest-addressed versions in the data root under researcher control", "egress": "explicit local download or an explicitly built export pack only", "redaction": "no content in logs; identifiers and checksums permitted"},
    {"id": "reviews", "category": "research", "sensitive": true, "classification": "confidential", "retention": "with the pinned artifact lifecycle in the local store", "egress": "local surfaces and explicitly selected exports only", "redaction": "claim text excluded from operational logs; finding id and verdict permitted"},
    {"id": "exports", "category": "research", "sensitive": true, "classification": "confidential", "retention": "owner-only export packs until the researcher reclaims them", "egress": "a file the researcher moves personally; the product performs no export upload", "redaction": "exclude tokens, credentials, keys, and unselected versions"},
    {"id": "operational_logs", "category": "operations", "sensitive": true, "classification": "restricted", "retention": "local log files inside the data root; no external log shipping", "egress": "none; logs never leave the machine", "redaction": "allowlist request/resource ids, status, latency, redacted error code; no research content or secrets"}
  ]
}
